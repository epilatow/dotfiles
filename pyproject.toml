[project]
name = "dotfiles"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "epilatow-repo-shared",
    "pytest>=9.0.3",
    "websockets>=16,<17",
]

[tool.uv.sources]
epilatow-repo-shared = { git = "https://github.com/epilatow/repo-shared", rev = "95b7151c35e5b199e9097fd3a8e3f79cdb7b390b" }

[tool.pytest.ini_options]
testpaths = ["_repo_shared/tests", "tests"]

[tool.mypy]
mypy_path = ["files/local/libexec/codex-archive-inactive"]

[tool.repo-shared.code-quality]
python-targets = [
    "files/local/libexec/tmux-agent-session-namer/tmux-agent-session-namer",
]
