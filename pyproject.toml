[project]
name = "dotfiles"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "epilatow-repo-shared",
    "pytest>=9.0.3",
]

[tool.uv.sources]
epilatow-repo-shared = { git = "https://github.com/epilatow/repo-shared", rev = "f8f9e51716df259cab9194f63d75bc64d33097e6" }

[tool.pytest.ini_options]
testpaths = ["_repo_shared/tests", "tests"]

[tool.repo-shared.code-quality]
python-targets = [
    "files/local/libexec/tmux-agent-session-namer/tmux-agent-session-namer",
]
